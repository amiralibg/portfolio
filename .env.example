# Frontend build-time config. Copy to .env for local dev.

# Where the contact form POSTs. Leave unset to use the same-origin
# /api/contact path (requires the nginx proxy block — see nginx.conf).
# VITE_CONTACT_API=http://localhost:8000/api/contact
