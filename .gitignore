# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — the contact API's admin token and bot token live here.
.env
.env.*
!.env.example

# The contact inbox. Real messages from real people; belongs in the deployed
# volume, never in the repo. (-wal/-shm are SQLite's write-ahead log sidecars.)
server/data/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
